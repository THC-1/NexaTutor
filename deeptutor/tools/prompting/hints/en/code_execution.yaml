short_description: "Run explicitly enabled Python in a system-isolated runner; returns stdout/stderr."
when_to_use: "Use when computation, algorithm checking, or numerical verification is more reliable than mental calculation."
input_format: "`language: python` + `code` — complete source using allowed imports. Optional `timeout` (seconds, default 30)."
guideline: "Write complete source yourself and print results to stdout. Use code execution for objective verification, not as a substitute for explaining reasoning."
note: "Unavailable without a healthy SYSTEM-isolated argv runner. Shell, subprocess and network imports are denied. This is risk reduction, not a strong sandbox for malicious code."
phase: "verification"
aliases:
  - name: run_code
    description: "Run Python for calculation, verification, or visualization."
    input_format: "`language: python` + `code`; optional `timeout`."
    when_to_use: "Use for objective checks, quick calculations, or small experiments."
    phase: "verification"
